//import { useState, useEffect } from "react";
//import axios from "axios";

const Bins = () => {
//  const [smartBins, setSmartBins] = useState([]);

//  useEffect(() => {
//    axios
//      .get("/api/smart-bins")
//      .then((response) => setSmartBins(response.data))
//      .catch((error) => console.error("Error fetching smart bin data:", error));
//  }, []);

  return (
    <div>
      <h1>Smart Waste Management</h1>
    
    </div>
  );
};

export default Bins;
